# Legacy Python implementation

`ghost_dl.py` is the original Python 3 downloader (v1.x). The supported
program is the C rewrite in the repository root (`make`).

This copy is kept only as a reference for behaviour.
